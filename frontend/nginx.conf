server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Route all requests through index.html so React Router handles them
    location / {
        try_files $uri $uri/ /index.html;
    }
}
